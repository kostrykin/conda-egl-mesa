name: test
channels:
  - conda-forge
dependencies:
  - python ~=3.12

  # ---------------------------------------------------------------------------
  # Build dependencies

  - libgl-devel
  - libegl-devel
  - libopengl-devel
  - libglu
  - cxx-compiler
  - make
  - cmake

  # ---------------------------------------------------------------------------
  # Run dependencies

  - mesalib
  - libegl
  - libgl
